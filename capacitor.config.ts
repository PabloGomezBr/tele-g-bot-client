import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.zenoy.projects',
  appName: 'Tele-G-Bot',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
